<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discover Mountains</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h2 class="mb-4 text-center">Discover Mountains</h2>

        {{> message }}

        {{#if mountains.length}}
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                {{#each mountains}}
                    <div class="col">
                        <div class="card h-100">
                            <img src="{{this.image}}" class="card-img-top" alt="{{this.name}}">
                            <div class="card-body">
                                <h5 class="card-title">{{this.name}}</h5>
                                <p class="card-text"><strong>Height:</strong> {{this.height}} meters</p>
                                <p class="card-text"><strong>Location:</strong> {{this.location}}</p>
                                <a href="{{this.url}}" class="btn btn-primary" target="_blank">Learn More</a>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        {{else}}
            <p class="text-center">No mountains found. Try again later.</p>
        {{/if}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
